services:
  db:
    image: postgres:latest
    environment:
      - POSTGRES_DB=gasjadb
      - POSTGRES_PASSWORD=gasjaapp
      - POSTGRES_USER=postgres
    ports:
      - '5532:5432'

  adminer:
    image: adminer
    ports:
      - '8081:8080'
    depends_on:
      - db